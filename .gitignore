# Dependencies
/node_modules

# Build output
/dist
/src-tauri/target
*.tsbuildinfo

# Game data & generated artifacts (large, reproducible — not tracked)
/games
/3K
/output
/reference

# Updater signing keys (private key must never be committed; pub is embedded in tauri.conf.json)
/src-tauri/.tauri-updater.key
/src-tauri/.tauri-updater.key.pub

# Local backend secrets (bug-report webhook etc.); .env.example is the committed template
/src-tauri/.env

# Editor / OS cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
